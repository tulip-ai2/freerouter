builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/"
port = 20128

[environment]
JWT_SECRET = "CHANGE_ME"
NODE_ENV = "production"
